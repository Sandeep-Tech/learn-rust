fn main() {
    
    // most others are the same as in other languages.
    
    // tuples
    // tuple is a collection of valyes of different types.

    // fn reverse(pair: (i32, bool)) -> (bool, i32) {
    //     // `let` can be used to bind the members of a tuple to variables
    //     let (integer, boolean) = pair;
    
    //     (boolean, integer)
    // }

    // print!(reverse(32, true));

    

}